# Security Policy

## Scope

این پروژه Packet و Socket سیستم‌عامل را در سطح پایین پردازش می‌کند و برای اجرا به دسترسی Administrator نیاز دارد. گزارش‌های امنیتی مرتبط با crash، resource exhaustion، بازماندن Socket، عبور ناخواسته داده و خطاهای WinDivert در اولویت هستند.

## Reporting

لطفاً جزئیات آسیب‌پذیری را پیش از انتشار عمومی از طریق [@patterniha](https://t.me/patterniha) ارسال کنید و موارد زیر را بنویسید:

- نسخه Windows و Python
- commit یا release مورد آزمایش
- مراحل بازتولید حداقلی
- اثر امنیتی مشاهده‌شده
- Log سانسورشده و بدون IP، شناسه یا Payload کاربران

اطلاعات ورود، Token، Subscription URL یا داده شخصی کاربران را در Issue عمومی قرار ندهید.
